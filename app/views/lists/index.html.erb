<%= image_tag "todo.jpg" %>

<h1>Lists</h1>

    <ul>
      <% @lists.each do |list| %>
        <li><%= link_to list.name, list_path(list) %> <button><%= link_to "Edit List", edit_list_path(list) %></button> </li>
      <% end %>
    </ul>

    <hr>


    <%= link_to "New List", new_list_path %>
