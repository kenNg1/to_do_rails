<%= render('layouts/errors', :object => @task) %>

    <%= form_for [@task.list, @task] do |f| %>
      <%= f.label :description %>
      <%= f.text_field :description %>
      <%= f.label :done %>
      <%= f.check_box :done %>
      <p><%= f.submit %></p>
    <% end %>
